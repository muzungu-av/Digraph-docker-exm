2. Устойчивость
Кластеризация Dgraph: использовать Dgraph в режиме кластера (Alpha и Zero) для отказоустойчивости и распределённого хранения.
Репликация данных: включить репликацию данных между узлами кластера.
Восстановление после сбоев: обеспечить возможность восстановления из резервной копии в случае сбоя.
Мониторинг и алертинг

3. Надёжность
транзакции
Повторные попытки записи: реализовать логику retry при ошибках записи/чтения.
Валидация данных (Вручную): валидация на уровне приложения или схемы Dgraph (если применимо).
Логирование и аудит: все транзакции и операции с кошельками должны быть зафиксированы в логах.

4. Персистентность данных
Регулярные бэкапы: автоматическое резервное копирование данных Dgraph (например, через dgraph backup).
Гарантированная запись на диск: убедиться, что все транзакции фиксируются на диск (fsync).
Восстановление из бэкапа: регулярные тесты восстановления из бэкапа.

5. Безопасность
• Аутентификация и авторизация: ограничить доступ к Dgraph и приложению с использованием ролей и токенов.
• Шифрование данных: шифрование на транспорте (TLS)
• Защита от SQL-подобных инъекций: Dgraph использует GraphQL+ -проводить валидацию запросов.

6. Производительность
    • Индексация в Dgraph: правильно настроить схему Dgraph с индексами по часто запрашиваемым полям

7. Интеграции
    • API для внешних систем: REST или GraphQL API для интеграции с другими сервисами (например, обработчиками транзакций, веб-интерфейсом).
    • Поддержка разных сетей: возможность переключения между тестовой и реальной сетью (testnet/mainnet).
    • Интеграция с блокчейном: возможность получения данных о транзакциях из внешних API

8. Гибкость и поддержка
    •Логирование и трассировка: структурированное логирование (например, в формате JSON)
    •Модульная архитектура: разделение на модули (например, WalletService, TransactionService, DgraphService) для удобства тестирования и сопровождения.

---(опционально)
9. Графовая модель
    • Схема Dgraph: отразить сущности:
        ◦ User (менеджер, партнёр, сотрудник)
        ◦ Wallet (криптокошелёк)
        ◦ Transaction (входящая, исходящая, внутренняя)
        ◦ Payment (платёж, связанный с транзакцией)
        ◦ Network (testnet/mainnet)
    • Связи: корректно смоделировать связи между сущностями (например, user -> wallet, wallet -> transaction, transaction -> payment).
    • История изменений: при необходимости, хранить историю состояний транзакций (например, pending, confirmed, failed).










=======================
наши исходные данные таковы:
- блокчейн TON
- на первом этапе один входящий кошелек, с возможностью сделать больше
- средства с этого кошелька переходят на несколько других в процентном соотношении согласно некоторой схеме (дерево кошельков). это внутренние кошельки компании для разных нужд.
- именно эта схема (несколько версий) хранится в БД Dgraph  , с указанием рабочей версии и способностями переключаться и работать по старым схемам, (есть как возможность хранить все версии в БД а работать по указанной, так и возможность удалять старые схемы если они по ним сейчас не происходит транзакции.
- возможность проверить проходит ли по схеме какая-то транзакция.
- если транзакция не проходит по схеме возможность ее закрыть, пометить недоступной, так что ее нельзя больше использовать.
- только одна версия схемы может быть рабочей
- при этом тестовая сеть и реальная, самостоятельные и независимые, у них у каждой свои рабочии версии
- ребра между звеньями (то есть платежи между кошельками) могут быть однонаправленными и двунаправленными


с учетом этого перечисли подходящую сущьности Схемы Dgraph



Сущности (Nodes)
1. Wallet
Поля:
id: string — уникальный ID кошелька (адрес в TON).
name: string — имя/описание кошелька (например, "main_incoming", "marketing_wallet").
type: string — тип кошелька (incoming, internal, outgoing).
network: string — сеть (testnet, mainnet).
status: string — статус (active, frozen, disabled).
created_at: datetime — дата создания.
updated_at: datetime — дата последнего обновления.

2. Transaction
Поля:
tx_hash: string — хэш транзакции.
from_wallet_id: string — ID отправителя.
to_wallet_id: string — ID получателя.
amount: float — сумма.
network: string — сеть (testnet, mainnet).
status: string — (pending, confirmed, rejected, invalid).
timestamp: datetime — время транзакции.
version_scheme_id: string — ID версии схемы, по которой проходила транзакция (если прошла по схеме).

3. SchemeVersion
Поля:
id: string — ID версии схемы.
name: string — имя схемы (например, v1.0, v2.0).
network: string — (testnet, mainnet).
is_active: bool — активна ли схема (только одна может быть true для каждой сети).
created_at: datetime — дата создания.
updated_at: datetime — дата обновления.
status: string — (active, archived, draft).

4. SchemeNode
Поля:
id: string — ID узла схемы (не путать с Wallet).
wallet_id: string — ID связанного кошелька.
percentage: float — процент распределения (если применимо).
scheme_version_id: string — к какой версии схемы относится.
node_type: string — (incoming, internal, leaf, root).
parent_node_id: string — ID родительского узла (для построения дерева).
direction: string — (one-way, two-way) — тип связи.

----------------
5. Связи (Edges)

1. Transaction связывает Wallet и Wallet
from_wallet → Transaction → to_wallet

2. SchemeVersion содержит SchemeNode
SchemeVersion → SchemeNode (одна схема может содержать много узлов)

3. SchemeNode связан с Wallet
SchemeNode → Wallet

4. SchemeNode связан с родительским SchemeNode
SchemeNode → parent_node_id (для построения дерева)

5. Transaction может быть связана с версией схемы
Transaction → version_scheme_id (если прошла по схеме)
--------------

🧱 Зачем нужен SchemeNode?
SchemeNode — это узел схемы, который описывает структуру дерева переводов. Он не является кошельком, но связывает кошелёк с определённой версией схемы и хранит информацию о том, как деньги должны распределяться.

🔍 Зачем он нужен:
1. Связывает Wallet с SchemeVersion
Не все кошельки участвуют в схеме, и не во всех схемах.
SchemeNode позволяет привязать конкретный кошелёк к конкретной версии схемы.
2. Хранит процент распределения (percentage: float)
Это ключевая часть логики: например, 50% идёт на кошелёк A, 30% — на B, 20% — на C.
Эти данные не могут храниться в Wallet, потому что один и тот же кошелёк может участвовать в разных схемах с разными процентами.
3. Позволяет строить дерево
У SchemeNode есть поле parent_node_id, что позволяет строить иерархию (дерево).
Например: incoming_wallet → marketing_wallet (30%) → social_ads_wallet (10%).
4. Определяет направление связи (direction: one-way | two-way)
Это позволяет моделировать односторонние и двусторонние переводы между узлами.

🌳 Пример дерева:

incoming_wallet (100%)
     |
     ├── marketing_wallet (30%)
     ├── dev_wallet (50%)
     └── ops_wallet (20%)
Каждый узел этого дерева — это SchemeNode, связанный с Wallet.

SchemeNode — это узел, описывающий позицию кошелька в схеме.
Он хранит процент, связь с родительским узлом, направление, и привязку к версии схемы.
Именно из SchemeNode-ов строится дерево распределения платежей.

📌 Упрощённо:
Wallet — это абстрактный кошелёк (адрес, имя, статус).
SchemeNode — это инстанс кошелька в схеме (какую роль он играет, с каким процентом, с кем связан в дереве).






